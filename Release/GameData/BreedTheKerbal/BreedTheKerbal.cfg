// BreedTheKerbal configuration
// All durations are in Kerbin seconds (1 day = 21 600 s).

BREEDTHEKERBAL_CONFIG
{
    // ── Pregnancy / life-stage durations ───────────────
    PregnancyDuration  =  648000     // 30 Kerbin days
    PostpartumDuration =  216000     // 10 days
    NewbornDuration    = 1080000     // 50 days
    ChildDuration      = 2160000     // 100 days
    TeenagerDuration   = 3240000     // 150 days

    // ── Death timers without a caretaker ───────────────
    NewbornDeathTimer  =  108000     // 5 days
    ChildDeathTimer    =  324000     // 15 days

    // ── Efficiency multipliers ─────────────────────────
    LatePregnancyEfficiency       = 0.40
    PostpartumEfficiency          = 0.00
    NewbornEfficiency             = 0.00
    ChildEfficiency               = 0.25
    TeenagerEfficiency            = 0.50
    NoCaretakerTeenagerEfficiency = 0.20
    LowSupportChildFactor         = 0.80
    LowSupportTeenagerFactor      = 0.70

    // ── Daily upkeep costs (career mode) ───────────────
    NewbornDailyCost  = 150
    ChildDailyCost    = 100
    TeenagerDailyCost =  50

    // ── Vessel requirements ────────────────────────────
    HabitatMinCrewCapacity = 4

    // ── Breeding mode ──────────────────────────────────
    AutoBreeding = false

    // ── Cupola aging bonus ─────────────────────────────
    // Each Cupola Module on the vessel speeds up aging by X.
    CupolaAgingBonusPerUnit = 0.05     // +5% per cupola
    CupolaAgingMaxCount     = 3        // at most 3 cupolas count toward the bonus

    // Landed / splashed bonus
    LandedAgingBonus        = 0.10     // +10% when Landed or Splashed

    // CommNet: active connection to home
    CommNetBonus            = 0.02     // +2% whenever connected to home

    // Bonus / penalty for parents on board
    BothParentsAgingBonus   = 0.05     // both parents on board: +5%
    NoParentsAgingPenalty   = 0.05     // no parents on board:   -5%
    //                                    one parent present:    ±0%

    // Caretaker trait: best adult on the vessel
    ScientistCaretakerBonus = 0.05     // Scientist as caretaker: +5%
    EngineerCaretakerBonus  = 0.03     // Engineer as caretaker:  +3%
    //                                    Pilot:                  ±0%

    // Vessel overcrowding: crew > OvercrowdingThreshold of capacity
    OvercrowdingThreshold   = 0.75     // threshold: 75% occupancy
    OvercrowdingPenalty     = 0.05     // penalty:   -5%

    // Low Support (insufficient funds in career mode)
    LowSupportAgingPenalty  = 0.10     // -10% aging speed
}
